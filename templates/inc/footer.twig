<div class="footer{{ not footer_widgets ? '--empty' : ''}}">
  {% if footer_widgets == '' %}
    <!-- Add some widgets to the footer -->
  {% else %}
  <div class="footer__content">
    {{footer_widgets}}
  </div>
  {% endif %}
  <div class="footer__site-info">
    <div class="site-info">
    {{ site_info }}
    </div>
  </div>
</div>